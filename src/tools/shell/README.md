# Shell Tool

## Purpose
Allows the agent to execute shell commands within the sandbox environment, providing CLI-based automation and system access.

## Maintenance Note
- This tool is currently disabled in `src/tools/mod.rs` for security reasons. Enable only with caution in restricted environments.
